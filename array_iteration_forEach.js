const numbers = [12,45,3,7,15];

const each = numbers.forEach((value, index, array) => console.log(value));
// takes 3 arguements: value,index and array(the array itself)
console.log(new_numbers);
console.log(each);